services:
  - type: web
    name: justchat-backend
    rootDirectory: backend
    runtime: node
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: PORT
        value: 3000
